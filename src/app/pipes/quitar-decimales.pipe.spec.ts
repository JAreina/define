import { QuitarDecimalesPipe } from './quitar-decimales.pipe';

describe('QuitarDecimalesPipe', () => {
  it('create an instance', () => {
    const pipe = new QuitarDecimalesPipe();
    expect(pipe).toBeTruthy();
  });
});
